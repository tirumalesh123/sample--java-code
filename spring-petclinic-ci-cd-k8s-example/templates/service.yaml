apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-petclinic-ci-cd-k8s-example.fullname" . }}
  labels:
    {{- include "spring-petclinic-ci-cd-k8s-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spring-petclinic-ci-cd-k8s-example.selectorLabels" . | nindent 4 }}
