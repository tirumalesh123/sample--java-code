apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spring-petclinic-ci-cd-k8s-example.fullname" . }}-test-connection"
  labels:
    {{- include "spring-petclinic-ci-cd-k8s-example.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spring-petclinic-ci-cd-k8s-example.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
